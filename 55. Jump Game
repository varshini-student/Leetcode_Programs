class Solution {

    public static boolean canJump(int[] arr) {
        int maxLen = 0;

        for (int i = 0; i < arr.length; i++) {
            if (i > maxLen) {
                return false;
            }
            maxLen = Math.max(maxLen, i + arr[i]);
        }

        return true;
    }
